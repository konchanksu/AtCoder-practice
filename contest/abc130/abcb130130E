#include<stdio.h>

int main(){
  int n, m;
  int i,j;
  
  if(n<1)return 0;
  if(m>2000)return 0;

  int s[n], t[m];
  for(i=0;i<n;i++){
    scanf("%d",s[i]);
  }

  for(i=0;i<m;i++){
    scanf("%d",t[i]);
  }


  return 0;
}
